export class Detail {

    detailId: number;
    detailText: string;

    constructor(detailId: number, detailText: string) {
        this.detailId = detailId;
        this.detailText = detailText;
    }
}
